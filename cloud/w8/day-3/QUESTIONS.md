# Questions for Mentor Minh

1. Trong project team thật, khi nào nên tách Terraform code thành module riêng?
2. Nếu Terraform state bị mất hoặc bị conflict thì quy trình xử lý an toàn là gì?
3. Với AWS project mới, nên dùng S3 native locking hay vẫn dùng DynamoDB lock?
4. Khi nào nên tách folder theo environment, khi nào nên dùng Terraform workspace?
5. Trong capstone nhỏ, có nên setup remote state từ đầu không hay local state là đủ?
